import { LayoutHeaderComponent } from './layout-header.component';

describe('LayoutHeaderComponent', () => {
  let component: LayoutHeaderComponent;

  beforeEach(async () => {
    component = new LayoutHeaderComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
