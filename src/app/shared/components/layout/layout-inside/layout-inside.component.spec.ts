import { LayoutInsideComponent } from './layout-inside.component';

describe('LayoutInsideComponent', () => {
  let component: LayoutInsideComponent;

  beforeEach(async () => {
    component = new LayoutInsideComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
