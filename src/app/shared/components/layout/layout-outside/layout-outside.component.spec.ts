import { LayoutOutsideComponent } from './layout-outside.component';

describe('LayoutOutsideComponent', () => {
  let component: LayoutOutsideComponent;

  beforeEach(async () => {
    component = new LayoutOutsideComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
